<div class="jumbotron text-center">
    <h1>{{headerMessage}}</h1>

    <br/>
    <br/>

    <p>Registration Form</p>

    <div class="form-horizontal form-group form-group-lg row">

        <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
            <h4>Username</h4>
            {{input type="email" value=email class="form-control"  autofocus="autofocus"}}
        </div>
        <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
            <h4>Password</h4>
            {{input type="password" value=password class="form-control" autofocus="autofocus"}}
        </div>
        <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
            <br/>
            <button class="btn btn-primary btn-lg btn-block" {{action 'registerToDB'}}>Register</button>
            {{#link-to 'posts' class="btn btn-primary btn-lg btn-block" tagName='button'}} Return {{/link-to}}
        </div>
    </div>

    {{#if responseMessage}}
    <div class="alert alert-success">{{responseMessage}}</div>
    {{/if}}

    <br/>
    <br/>

</div>
